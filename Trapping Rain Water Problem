height = [3, 0, 2, 0, 4]

water = 0

for i in range(1, len(height)-1):

    left = max(height[:i])
    right = max(height[i+1:])

    water += max(0, min(left, right) - height[i])

print(water)

Output

7
